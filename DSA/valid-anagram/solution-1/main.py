class Solution:
    def isAnagram(self, s: str, t: str) -> bool:
        #check if the length of the two strings are equal if not return false
        #sort the characters of both strings and compare them if they are equal return true otherwise return false
        