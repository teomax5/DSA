class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        #loop through the list of numbers
        #for imth number loop through the list of numbers starting from the next number
            #check if the sum of the two numbers is equal to the target if it is return the indices of the two numbers
        #if no such numbers are found return an empty list