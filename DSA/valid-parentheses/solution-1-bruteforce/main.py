class Solution:
    def isValid(self, s: str) -> bool:
        #while loop the string unitl it has'()' or '[]' or '{}'
            #replace the '()' or '[]' or '{}' with an empty string
        #if the string is empty return true otherwise return false